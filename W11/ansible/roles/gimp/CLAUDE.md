# Rol `gimp`

## Qué hace
Instala **GIMP** con Chocolatey (`gimp`), fijando `gimp_version` (por defecto `3.2.4`).

## Estructura
- `tasks/main.yml`
- `defaults/main.yml` → `gimp_version` (vaciar para la última).

## Notas
- Alternativa: winget `GIMP.GIMP`.
