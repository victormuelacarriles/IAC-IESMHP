# Rol `zoomit`

## Qué hace
Instala **ZoomIt** (utilidad de zoom/anotación de Sysinternals) con Chocolatey
(`zoomit`). Sin versión fija.

## Estructura
- `tasks/main.yml` — sin `defaults/`.

## Notas
- Alternativa: winget `Microsoft.Sysinternals.ZoomIt`.
