#Para montar un usb en la sesion de thinclient

#Localizar el usb  (sde1?)
dmesg | grep -E "sd[a-z]"

#ó  cat /proc/partitions
#ó  ls -l /dev/disk/by-id/
#ó  ls -l /dev/disk/by-uuid/
#porque  "lsblk"  falla en thinclient

#Encontrado el usb  

sudo mkdir -p /media/mi_usb
sudo mount /dev/sde1 /media/mi_usb
cd /media/mi_usb