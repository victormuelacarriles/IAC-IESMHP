# 2026-06-15 — Cambios

## (20:30) Reloj/NTP del rol `preparaAD`: relajar `maxdistance` (chrony rechazaba el DC)

**Motivo**: con el fix de las 18:45 (armar `makestep 0.1 3` + `waitsync`) la
unión **SEGUÍA fallando** en el equipo Mint con `NTPSynchronized=no`. El
`chronyc sources -v` lo destapó:

```
^? 10.0.72.118    1   6   377     6   +7202s[ +7202s] +/-  10.3s
```

- `Reach 377` (octal) = los 8 sondeos respondieron → **conectividad con el DC
  perfecta** (no era firewall/routing).
- `^?` = chrony marca la fuente **inutilizable**; por eso `chronyc tracking`
  daba `Reference ID 00000000 / Stratum 0 / Not synchronised` → chrony **nunca
  selecciona** el DC.
- `+/- 10.3s` = la **distancia de raíz** de la fuente (~10 s) supera el
  **`maxdistance` por defecto de chrony (3.0 s)** → rechazo.

Causa raíz: **el DC Windows (w32time) anuncia una dispersión de raíz enorme
(~10 s)** —coherente con `offset +7202s`: su reloj CMOS ha derivado ~2 h porque
el w32time **no sincroniza con ningún NTP fiable**—. chrony, correctamente,
descarta una fuente de tan baja calidad por `maxdistance`. Con la fuente
descartada, `makestep`/`waitsync` no tienen nada que seleccionar: el armar+esperar
del fix anterior era correcto, **faltaba hacer el DC *seleccionable***.

### Modificado

- **`roles/preparaAD/tasks/main.yml`** — bloque "3. Reloj", `blockinfile` de
  chrony: tras los `server ... iburst prefer trust` se añade **`maxdistance
  16.0`** (con comentario explicando el porqué). Así chrony acepta el DC pese a
  su dispersión y `waitsync` sincroniza. Idempotente: el marcador del bloque es
  el mismo, solo cambia el contenido → en re-pases actualiza el bloque, reinicia
  chrony y vuelve a sincronizar. Para Kerberos basta < 5 min, así que relajar
  `maxdistance` no tiene inconveniente en este uso.

### Workaround manual aplicado en el equipo Mint (para desbloquear ya)

```bash
echo 'maxdistance 16.0' | sudo tee -a /etc/chrony/chrony.conf
sudo systemctl restart chrony
sudo chronyc makestep 0.1 3
sudo chronyc waitsync 30 0.05 0 1
timedatectl status        # System clock synchronized: yes
```

### Arreglo de fondo (DC, recomendado)

El DC va ~2 h adelantado: **sincronizar el w32time del DC con un NTP fiable**
(o, en el dominio de pruebas, al menos fijar su hora). Si no, TODOS los equipos
del dominio quedarán ~2 h desfasados respecto a la hora real (correcto para
Kerberos, molesto para logs/timestamps). `maxdistance 16.0` es la red de
seguridad del lado cliente, no sustituye a arreglar el DC.

### Comprobado

- YAML válido en `tasks/main.yml`; el bloque chrony queda con los `server` + la
  línea `maxdistance 16.0`.
- Workaround manual reproducido en el Mint: tras `maxdistance 16.0` + restart,
  `chronyc sources -v` pasa a `^*`/`^+` y `timedatectl` → `synchronized: yes`.

### Pendiente (al desplegar)

- commit+push a `main` para que los equipos clonen el `tasks/main.yml` nuevo.
- Si la dispersión del DC fluctuara > 16 s, subir a `maxdistance 30` (en
  `entornoAD.yml` no hay knob; se edita el bloque del rol).

---

## (18:45) Reloj/NTP del rol `preparaAD`: ESPERAR a la sincronización antes de unir

**Motivo**: tras el cambio de las 14:30 (soporte chrony + `chronyc makestep`),
la unión de un equipo **seguía fallando** con el mensaje de *desfase de reloj* y
dejando el equipo *"unido pero sin poder autenticar"* (join a medias). Causa
raíz: **el `makestep` se lanzaba inmediatamente tras `systemctl restart chrony`,
cuando chrony todavía NO tiene ninguna muestra del NTP** (el `iburst` tarda unos
segundos) → `makestep` era un **no-op** y el reloj seguía desfasado cuando, acto
seguido, corría `realm join`. Por eso el workaround **manual** del 14:30 sí
funcionó (al teclearlo a mano ya habían pasado segundos y chrony tenía muestra).

### Modificado

- **`roles/preparaAD/tasks/main.yml`** — bloque "3. Reloj":
  - **chrony**: el único `chronyc makestep` se sustituye por **armar + esperar**:
    `chronyc makestep 0.1 3` (step —no slew— en las próximas muestras) seguido de
    `chronyc waitsync 30 0.05 0 1` (bloquea hasta sincronizar o ~30 s). Ambos
    `failed_when: false`.
  - **systemd-timesyncd**: tras el restart, nueva tarea que **sondea**
    `timedatectl show -p NTPSynchronized --value` con `until: == 'yes'`,
    `retries: 10`, `delay: 3` (máx ~30 s). Sin `when` (en re-pases confirma el
    reloj sin reiniciar el servicio).
  - **Resumen**: la línea "Reloj sincronizado por NTP" pasa de volcar el `yes/no`
    crudo a **SÍ / NO con pista accionable** (NTP/conectividad al DC; avisa si
    `preparaad_ntp` está vacío).
- **`roles/preparaAD/utilesAD/3-UneAlDominio.sh`** — nueva **guarda de reloj
  (paso 3b)**, tras `realm discover` y **antes** de pedir credenciales: si
  `NTPSynchronized != yes`, fuerza el ajuste (chrony `makestep`+`waitsync`, o
  reinicia timesyncd y sondea); si sigue desfasado, **aborta** con diagnóstico
  (`timedatectl status`, `chronyc tracking/sources`) **sin crear el objeto de
  equipo huérfano** en AD. Red de seguridad por si el rol no corrigió a tiempo.
- **`roles/preparaAD/utilesAD/entornoAD.sh`** — exporta también `NTP`
  (`preparaad_ntp`), usado como pista en los mensajes de la guarda. Cabecera
  actualizada.
- **Doc**: `CLAUDE.md` del rol (sección 3 reescrita con armar+esperar y el
  no-op del makestep inmediato; tabla de scripts con la guarda de reloj; nota
  nueva en Estado/Notas "Reloj = requisito DURO" con el síntoma del join a
  medias y la limpieza del huérfano). `LeemeComoUnirAlDominio.md` (punto 3 de la
  sección 1 actualizado a chrony/timesyncd + espera; sección 6 punto 4 "NTP no
  opcional"; **nueva sección 7** "Problema típico: reloj desfasado → join a
  medias" con diagnóstico, ajuste manual y limpieza del objeto huérfano).

### Comprobado

- `bash -n` OK en `3-UneAlDominio.sh` y `entornoAD.sh`; YAML válido en
  `tasks/main.yml`.
- `entornoAD.sh` exporta `NTP=10.0.72.118` (además de DOMINIO/OU/USUARIO/DNSS).

### Pendiente (al desplegar)

- commit+push a `main` para que los equipos clonen `tasks/main.yml`,
  `3-UneAlDominio.sh` y `entornoAD.sh` nuevos.
- Verificar en el equipo Mint que reproducía el fallo: lanzar
  `3-UneAlDominio.sh` y confirmar `[OK] Reloj sincronizado por NTP.` antes del
  join (y, si había objeto huérfano, limpiarlo con `adcli delete-computer`).

---

## (14:30) Reloj/NTP del rol `preparaAD`: soporta chrony **o** systemd-timesyncd

**Motivo**: probando la unión de un equipo **Linux Mint** al dominio de pruebas
`mhpies.local`, `realm join` falló con *"Failed to join the domain"* **pero el
objeto de equipo sí se creó** en `OU=ComputersLinux` (join a medias: la fase
LDAP crea la cuenta; la fase de finalización —keytab/auth como cuenta de
máquina— falló y `adcli` abortó dejando el objeto huérfano). Causa: **reloj
desfasado** (`timedatectl` → *System clock synchronized: no*). Dos problemas de
fondo:

1. `preparaad_ntp` estaba **vacío** en `entornoAD.yml`, así que el rol nunca
   apuntaba el reloj al DC (las tareas de NTP se saltaban).
2. El rol solo sabía configurar **systemd-timesyncd**, que en **Mint no existe**
   (usa **chrony**; por eso `/etc/systemd/timesyncd.conf` no estaba y la unidad
   no aparecía, aunque `timedatectl` decía *NTP service: active*). El pool de
   internet de chrony no se alcanza desde el aula → el reloj nunca sincronizaba.

### Modificado

- **`roles/preparaAD/entornoAD.yml`** — `preparaad_ntp: ""` → `"10.0.72.118"`
  (DC de pruebas). Cabecera y comentario actualizados con la pareja prod/test
  del NTP (`prod 10.0.1.48 / test 10.0.72.118`) y nota de que el rol detecta el
  cliente NTP. Acepta lista separada por comas.
- **`roles/preparaAD/tasks/main.yml`** — el bloque "3. Reloj" pasa de configurar
  solo timesyncd a **detectar el cliente NTP y configurar el que haya**, con
  prioridad a chrony:
  - Detección vía `systemctl list-unit-files --type=service` → facts
    `preparaad_ntp_chrony` / `preparaad_ntp_timesyncd` / `preparaad_chrony_svc`
    (`chrony.service` vs `chronyd.service`).
  - **chrony**: `blockinfile` con `server <ntp> iburst prefer trust` en
    `chrony.conf` (`/etc/chrony/chrony.conf` en Debian, `/etc/chrony.conf` en el
    resto; `create: false`), restart del servicio si cambió, y `chronyc makestep`
    (ajuste inmediato best-effort, para que la unión posterior no pille el reloj
    desfasado).
  - **timesyncd**: igual que antes, pero `NTP=` normaliza la lista
    (`split(',') | map('trim') | join(' ')`).
  - **ninguno**: tarea `debug` de aviso (no toca el reloj).
- **`roles/preparaAD/CLAUDE.md`** — sección 3 (Reloj) reescrita con las tres
  ramas; fila `preparaad_ntp` de la tabla con valores prod/test y la detección.

### Workaround manual aplicado en el equipo Mint (para desbloquear ya)

- `adcli delete-computer --domain=mhpies.local --login-user=svc-union-linux MINT`
  (limpia el objeto a medias en AD).
- chrony apuntado al DC a mano (`server 10.0.72.118 iburst prefer trust` +
  `systemctl restart chrony` + `chronyc makestep`) → reloj OK → reintentar
  `realm join` / `3-UneAlDominio.sh`.

### Comprobado

- YAML válido (`entornoAD.yml`, `tasks/main.yml`) y derivación de la OU intacta.
- Lógica de detección: con `chrony.service` presente → rama chrony; con solo
  `systemd-timesyncd.service` → rama timesyncd; con ninguno → aviso.

### Pendiente (al desplegar)

- commit+push a `main` para que los equipos clonen el `entornoAD.yml` y el
  `tasks/main.yml` nuevos.
- Producción: además del dominio/DNS, fijar `preparaad_ntp` al DC de producción
  (`10.0.1.48`).

---

## Nuevo `4-SacaDelDominio.sh` + delegación de borrado en `1-CreaUsuarioUnionAD.ps1`

**Motivo**: faltaba el inverso de `3-UneAlDominio.sh` (sacar un equipo del
dominio de forma guiada) y la cuenta de unión `svc-union-linux` no tenía
permiso para borrar la cuenta de equipo en AD.

### Fichero nuevo

- **`roles/preparaAD/utilesAD/4-SacaDelDominio.sh`** — inverso de
  `3-UneAlDominio.sh`. Como root: (1) comprueba si está unido (`realm list`; si
  no, termina); (2) pide confirmación; (3) pregunta la contraseña de
  `svc-union-linux` (en blanco = otro usuario del dominio con permisos de
  borrado); (4) `realm leave --unattended -U <usuario> <dominio>` (borra la
  cuenta de equipo de la OU en AD + deshace la config local) y verifica que
  `realm list` queda vacío; (5) elimina el snippet SSSD huérfano
  `/etc/sssd/conf.d/10-iac-ad.conf` (si no, sssd arrancaría como unidad fallida
  con un dominio sin sección `[sssd]`). Deja krb5/split-DNS/nsswitch intactos
  (facilitan una reunión posterior). Lee `USUARIO_UNION` de `entornoAD.yml` vía
  `entornoAD.sh` (mismo punto único de cambio). Pisa-able con
  `USUARIO_UNION=admin ./4-SacaDelDominio.sh`.

### Modificado

- **`roles/preparaAD/utilesAD/1-CreaUsuarioUnionAD.ps1`** — nueva ACE
  `DeleteChild` de la clase *computer* sobre la OU (mismo patrón e inheritance
  `All` que la `CreateChild` existente): delega el **borrado de equipos acotado
  a la OU `ComputersLinux`** (delegación estándar "crear/borrar cuentas de
  equipo"). Es lo que necesita `realm leave -U`. Reglas pasan de 5 a 6;
  idempotencia (comprobación ACE a ACE) intacta. Actualizados `.SYNOPSIS`,
  comentarios y resumen final ("unir Y sacar"; ya no dice "sin borrado").
- Doc: `CLAUDE.md` (tabla de scripts + nota "Salir del dominio") y
  `LeemeComoUnirAlDominio.md` (paso 1 de delegación + nueva subsección "Sacar UN
  equipo del dominio").

### Comprobado

- `bash -n` OK en los 4 scripts bash y en `entornoAD.sh`; `4-SacaDelDominio.sh`
  resuelve `USUARIO_UNION=svc-union-linux` vía `entornoAD.sh`.
- PS1: parsea sin errores; el lector de `entornoAD.yml` devuelve
  `Usuario=svc-union-linux`, `NombreOU=ComputersLinux` y fallback correcto para
  claves inexistentes.

---

## Centralización del dominio AD del rol `preparaAD` en `entornoAD.yml` (único punto de cambio)

**Motivo**: el dominio al que se une el equipo (y sus derivados: IPs de DNS y
OU) estaba **duplicado en varios sitios** del rol `preparaAD`, cada uno con su
pareja producción (`iesmhp.local`) / pruebas (`mhpies.local`) comentada a mano:
- `defaults/main.yml` → `preparaad_dominio`, `preparaad_dominio_dnss`, `preparaad_ou`
- `utilesAD/3-UneAlDominio.sh` → `DOMINIO`, `OU`
- `utilesAD/2-CreaVault.sh` → `USUARIO_UNION`
- `utilesAD/1-CreaUsuarioUnionAD.ps1` → `NombreOU`, `Usuario`

Conmutar entre el dominio de pruebas y el de producción obligaba a tocar 4
ficheros y era fácil dejar alguno descuadrado.

**Solución**: nuevo fichero **`roles/preparaAD/entornoAD.yml`** como **única
fuente de verdad** del entorno AD. Para cambiar el dominio (producción ⇄
pruebas) se edita SOLO ese fichero; la OU completa se **deriva** del dominio.

### Fichero nuevo

- **`roles/preparaAD/entornoAD.yml`** — define `preparaad_dominio`,
  `preparaad_dominio_dnss`, `preparaad_nombre_ou`, `preparaad_ou` (DERIVADA vía
  Jinja: `OU={{ nombre_ou }},DC={{ dominio.split('.') | join(',DC=') }}`),
  `preparaad_usuario_union` y `preparaad_ntp`. Lleva en la cabecera la pareja
  prod/test lista para conmutar. Valor activo: **pruebas** (`mhpies.local`).
- **`roles/preparaAD/utilesAD/entornoAD.sh`** — helper que `source`an los dos
  scripts bash: parsea `entornoAD.yml` (sed para escalares simples) y exporta
  `DOMINIO`, `NOMBRE_OU`, `OU` (derivada, `dominio.tld → DC=dominio,DC=tld`),
  `USUARIO_UNION`, `DOMINIO_DNSS`. Respeta valores ya puestos por entorno
  (p. ej. `DOMINIO=otro.local ./3-UneAlDominio.sh`).

### Cómo lo consume cada parte

- **Rol Ansible**: `tasks/main.yml` carga el fichero con `include_vars` como
  **tarea 0** (tags `preparaad, ad`). `-e` sigue pisando estos valores
  (extra-vars > include_vars). Templates (`krb5.conf.j2`, `10-iac-ad.conf.j2`)
  ya usaban `preparaad_dominio*` → sin cambios.
- **`utilesAD/3-UneAlDominio.sh`** y **`utilesAD/2-CreaVault.sh`**: dejan de
  tener el dominio/OU/usuario a fuego; hacen `source entornoAD.sh`.
- **`utilesAD/1-CreaUsuarioUnionAD.ps1`**: lee de `..\entornoAD.yml` los
  defaults de `Usuario`/`NombreOU` (fallback a los literales si el fichero no
  está, p. ej. script copiado suelto al DC). El **dominio NO** se lee de ahí:
  se sigue autodetectando con `Get-ADDomain` (correcto en un DC).

### Ficheros modificados

- `roles/preparaAD/defaults/main.yml` — eliminadas `preparaad_dominio`,
  `preparaad_dominio_dnss`, `preparaad_ou`, `preparaad_usuario_union`,
  `preparaad_ntp` (movidas a `entornoAD.yml`). Quedan solo los ajustes de
  **comportamiento** (`paquetes`, `arregla_nsswitch`, `unir`,
  `password_union`, `fqn`, `fallback_homedir`, `gpo`).
- `roles/preparaAD/tasks/main.yml` — nueva tarea 0 `include_vars`.
- `roles/preparaAD/utilesAD/{2-CreaVault,3-UneAlDominio}.sh` — `source` del helper.
- `roles/preparaAD/utilesAD/1-CreaUsuarioUnionAD.ps1` — params sin default fijo
  + lectura de `entornoAD.yml`.
- `roles/preparaAD/CLAUDE.md` y `LeemeComoUnirAlDominio.md` — doc actualizada:
  tabla de variables dividida en *entorno* (`entornoAD.yml`) vs *comportamiento*
  (`defaults/main.yml`); referencias reapuntadas al fichero único.

### Comprobado

- Parser bash: extrae `DOMINIO/NOMBRE_OU/OU/USUARIO_UNION/DOMINIO_DNSS`
  correctos del `entornoAD.yml` real; deriva `OU=ComputersLinux,DC=mhpies,DC=local`.
- Override por entorno (`DOMINIO=iesmhp.local`) respetado y OU recalculada.
- Simulación de producción (dominio `iesmhp.local`) → `OU=...,DC=iesmhp,DC=local`.
- YAML válido (`yaml.safe_load`) en `entornoAD.yml` y `defaults/main.yml`;
  derivación Jinja de la OU reproducida y correcta.

### Pendiente (al desplegar)

- `entornoAD.yml` es un fichero **nuevo**: hay que commit+push a `main` para que
  esté en el repo clonado de los equipos antes de lanzar el rol allí.
- Para producción: editar las dos primeras claves de `entornoAD.yml`
  (`preparaad_dominio: iesmhp.local`, `preparaad_dominio_dnss: 10.0.1.48,10.0.1.54`).
