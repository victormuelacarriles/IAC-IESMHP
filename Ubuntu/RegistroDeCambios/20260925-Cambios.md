# 2026-09-25 — Cambios

## 17:22 — `1-SetupLiveCD.sh`: apt-get queda DETENIDO al instalar zfsutils-linux (CEIABD)

**Síntoma**: en perfil CEIABD el log termina en
`Procesando disparadores para libc-bin ...` y el script no avanza (nunca aparece
`ZFS versión: ...`). La VM sigue accesible por SSH.

**Diagnóstico** (por SSH):
```
pstree -alp $(pgrep -of 1-SetupLiveCD)
  bash,5463 1-SetupLiveCD.sh
  |-apt-get,6076 install -y zfsutils-linux
  `-tee,5471 -a .../1-SetupLiveCD.sh.log
ps -eo pid,etime,stat,cmd → 6076  T+  apt-get install -y zfsutils-linux
```
`apt-get` no está colgado sino **detenido** (estado `T`) por una señal de control
de terminal: dpkg ya había terminado y apt se para al volver a tocar el tty
(stdin = terminal, stdout = pipe hacia `tee` por el `exec > >(tee ...)`).
El módulo zfs estaba cargado (`zfs 2.4.1-1ubuntu4`, `/dev/zfs` presente, `zed`
corriendo) → no es un problema de ZFS.

Contexto: esta vez `apt` **actualizó** zfsutils-linux (ya venía en el live,
2.4.1-1ubuntu5 → 2.4.1-1ubuntu5.1 de `resolute-updates`), algo que no pasaba en
las pruebas de junio.

**Desbloqueo en caliente**: `sudo kill -CONT <pid de apt-get>`.

**Fix**: `</dev/null` en los dos `apt-get` del bloque ZFS (update + install)
para que apt no tenga el terminal como stdin.

**17:26 — Verificado**: con el fix aplicado el bloque ZFS ya no se detiene y
`1-SetupLiveCD.sh` continúa normalmente. Problema **resuelto**.

## 17:31 — Mismo fallo, ahora en `2-SetupSOdesdeLiveCD.sh` (PASO 9)

**Síntoma**: con el fix anterior, `1-SetupLiveCD.sh` pasa el bloque ZFS, crea
los pools, hace el rsync y entra en el chroot. Se vuelve a parar en el PASO 9 de
`2-SetupSOdesdeLiveCD.sh` ("Instalar ZFS en el sistema instalado"), justo tras
`Procesando disparadores para libc-bin`, al final del `apt-get install` de ZFS.

**Causa**: la misma. El script 2 se lanza con
`chroot /mnt ... 2>&1 | tee ...` y hereda el terminal como stdin, así que sus
`apt-get` (líneas ~428, ~468, ~1039, ~1050) pueden quedar detenidos igual.

**Fix** (un solo punto en vez de parchear cada apt-get): en `1-SetupLiveCD.sh`,
la llamada al chroot pasa a
`chroot /mnt "$SCRIPT2" </dev/null 2>&1 | tee ...`.
Comprobado antes de aplicarlo que el script 2 no lee nada del teclado (todos sus
`read` leen de pipes o ficheros).

**Pendiente**: confirmar en la próxima ISO que el script 2 completa el PASO 9 y
llega a `Correcto`.

## 17:34 — Revisión del resto de scripts (mismo bug) y blindaje de toda la cadena

Patrón vulnerable: **stdin = terminal + stdout = pipe (`tee`) + `apt-get`
que ejecuta dpkg**. Revisados todos los scripts de `Ubuntu/ISO/26.04/`:

| Script | ¿Vulnerable? | Acción |
|---|---|---|
| `0a-CreaISO.sh` (equipo de desarrollo) | No: apt directo en terminal, sin pipe | — |
| `iac-iesmhp-run.sh` (heredoc en `0a-CreaISO.sh`) | Sus apt no (stdout = terminal). Pero lanza `0b-Github.sh ... \| tee` con stdin = terminal → **toda la cadena lo hereda** | `sudo /bin/bash /0b-Github.sh </dev/null 2>&1 \| tee ...` |
| `0b-Github.sh` | **Sí** (`exec > >(tee)` + 2 apt-get). No se había manifestado porque git ya viene en el live (no ejecuta dpkg) | `</dev/null` en los 2 apt-get y en `bash "$SCRIPT_INSTALL"` |
| `1-SetupLiveCD.sh` | Sí (ya corregido 17:22/17:31) | Además `exec </dev/null` global tras `loadkeys` (cubre apt/chroot futuros) |
| `2-SetupSOdesdeLiveCD.sh` | Sí, vía chroot (corregido 17:31) | Cubierto por el `exec </dev/null` del script 1 |
| `3-SetupPrimerInicio.sh` (+ `NombreIP.sh`, `Auto-Ansible.sh`, ansible, `4-Comprobaciones.sh`) | No bajo systemd (`StandardInput=null` por defecto) | `exec </dev/null` defensivo para re-ejecución manual desde terminal |
| `4-Comprobaciones.sh` | No: solo `dpkg -l`/`--audit` (consultas, sin dpkg de instalación) | — |

Ningún script lee del teclado (todos los `read` leen de pipes/ficheros), así que
cortar stdin no rompe nada. `bash -n` OK en los 4 ficheros tocados.

**⚠ Despliegue**: `1-` y `3-` se clonan de GitHub (basta commit+push). Pero
`0a-CreaISO.sh` (wrapper `iac-iesmhp-run.sh`) y `0b-Github.sh` van **embebidos en
la ISO** → sus cambios solo aplican al **regenerar la ISO**. Con la ISO actual
basta el fix del script 1 (el `exec </dev/null` protege 1 y 2).

**Versiones actualizadas** (`VERSIONSCRIPT`):
- `1-SetupLiveCD.sh`: `23.1-20260612-Ubuntu-zfs` → `23.2-20260925-Ubuntu-zfs`
- `3-SetupPrimerInicio.sh`: `22.24-20260520` → `22.25-20260925`
- `2-SetupSOdesdeLiveCD.sh` no se ha tocado (sigue `23.7-20260612-zfs-homeunico`).
- `0b-Github.sh`: **nuevo** `VERSIONSCRIPT="1.0-20260925"` (no tenía). Se
  muestra en las líneas de inicio y fin de `0b-Github.sh.log`, que se copia al
  sistema instalado → permite saber con qué ISO se arrancó cada equipo.
  Como va embebido en la ISO, subirla en cada cambio y regenerar la ISO.
- `0a-CreaISO.sh`: **nuevo** `VERSIONSCRIPT="1.0-20260925"` (por coherencia con
  el resto). Se muestra al generar la ISO, en "Verificando ficheros de entrada".

Observación menor: userland zfs 2.4.1-1ubuntu5.1 con módulo 2.4.1-1ubuntu4 del
kernel live (misma versión upstream 2.4.1, compatibles).

## 18:20 — Rol `clienteNAS`: elegir la interfaz del NAS según la red del equipo

El NAS tiene varias interfaces: `10.0.1.253` (general, para todo el centro) y
otras dedicadas a las redes principales (`10.0.72.253` para `10.0.72.0/24`,
`10.0.32.253` para `10.0.32.0/24`...). Antes el rol usaba una IP fija
(`nas_server_ip`).

- `defaults/main.yml`: nuevas `nas_ip_general` (`10.0.1.253`) y
  `nas_ips_por_red` (mapa `"10.0.72": "10.0.72.253"`, `"10.0.32": "10.0.32.253"`).
  `nas_server_ip` pasa a vacío por defecto y solo sirve para **forzar** una IP.
- `tasks/main.yml`: se calcula la red del equipo (3 primeros octetos de
  `ansible_default_ipv4.address`) y se elige `nas_ip`: la dedicada si la red está
  en el mapa, si no la general. Si la dedicada no responde a `showmount` tras los
  reintentos, **plan B** con la general (en el log sale el fallo como
  `...ignoring`). Si falla la general, el rol falla (como antes).
- Las entradas de `/etc/fstab` usan `nas_ip`. Como `lineinfile` busca por punto
  de montaje, en una re-ejecución con otra IP la línea se reescribe; el montaje
  ya activo no se rehace hasta reiniciar o `umount`/`mount`.
- Para añadir una red con interfaz propia: una línea más en `nas_ips_por_red`.

## 19:56 — Rol `clienteNAS`: excluir exports por palabra (`nas_excluir`)

- `defaults/main.yml`: nueva lista `nas_excluir` (`LiveCDs`, `OtroAexcluir`).
- `tasks/main.yml`: tras autodescubrir los exports, se separan los que contienen
  alguna palabra de la lista (búsqueda por subcadena en la ruta exportada, sin
  distinguir mayúsculas, con `regex_escape`) y se quitan de la lista a montar.
  Con la lista vacía no se excluye nada.
- Limpieza de re-ejecución: si un export excluido ya estaba montado, se
  desmonta (`mountpoint -q && umount`) y se borra su línea de `/etc/fstab`
  (buscada por punto de montaje, igual que al añadirla).
- `CLAUDE.md` del rol actualizado.
