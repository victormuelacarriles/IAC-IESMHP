# Cambios 2026-05-17

## Rol Ansible `rdp` — fix DEFINITIVO: desplegar la config en vez de usar `grdctl`

### Hora: ~13:00

**Contexto**: el 2026-05-16 se intentó arreglar que el RDP quedara `disabled`
tras el primer arranque cambiando el rol a `parar daemon → grdctl set-*/enable
→ arrancar` + verificación. Tras regenerar la ISO y reinstalar, **siguió sin
funcionar**.

**Diagnóstico (equipo `10.0.72.51`, Ubuntu 26.04, gnome-remote-desktop
`50.0-0ubuntu2`)**:

1. Log `5-PrimerArranque.log` (12:24:25-26): el código nuevo corrió
   (`TASK [rdp : Parar gnome-remote-desktop antes de configurar]`), y la tarea
   `Verificar que el servidor RDP quedó habilitado` hizo
   `fatal: ... failed_when_result: true` con `Status: disabled`,
   `TLS certificate: (null)`, `TLS key: (null)`, **`Password: (hidden)`** →
   `PLAY RECAP ... failed=1` (consecuencia: `vscode` y roles posteriores **no
   se ejecutaron**).
2. Re-aplicando a mano con el daemon **vivo** (`set-* → enable →
   systemctl restart`): `Status: enabled`, persiste tras reboot.

**Causa raíz**: con el daemon `gnome-remote-desktop` **parado**,
`grdctl --system rdp set-credentials` SÍ persiste (almacén GKeyFile propio,
independiente del daemon) pero `set-tls-cert`/`set-tls-key`/`enable` **NO**
(necesitan el daemon vivo). El 1er fix paraba el daemon → rompía justo la parte
tls/enable. Confirmado por el patrón `Password: hidden` + `TLS: null` +
`disabled`.

**Ficheros de referencia capturados** (máquina donde funcionó y persistió tras
reboot):

- `/etc/gnome-remote-desktop/grd.conf` (owner `gnome-remote-desktop`, `0664`):
  ```ini
  [RDP]
  tls-cert=/etc/gnome-remote-desktop/tls.crt
  tls-key=/etc/gnome-remote-desktop/tls.key
  enabled=true
  ```
- `…/.local/share/gnome-remote-desktop/credentials.ini` (owner
  `gnome-remote-desktop`, `0600`) — **texto plano**, portable:
  ```ini
  [RDP]
  credentials={'username': <'ubuntu'>, 'password': <'ubuntu'>}
  ```

### Cambios aplicados (fix definitivo)

Estrategia: **no usar `grdctl` para configurar** (solo para leer/verificar);
escribir directamente los dos ficheros, que son deterministas, y reafirmarlos
en cada arranque antes del daemon (patrón `iac-gdm-noautologin.service`).

1. **`roles/rdp/templates/iac-rdp-config.sh.j2`** (nuevo): script que escribe
   `grd.conf` (`enabled=true` + rutas TLS, `0664`) y `credentials.ini`
   (GVariant con `rdp_username`/`rdp_password`, `0700` el dir, `0600` el
   fichero), todo owner `gnome-remote-desktop`.
2. **`roles/rdp/files/iac-rdp-config.service`** (nuevo): unidad `oneshot`,
   `DefaultDependencies=no`, `Before=gnome-remote-desktop.service`,
   `After=local-fs.target`, `WantedBy=gnome-remote-desktop.service`.
3. **`roles/rdp/tasks/main.yml`** reescrito: instala pkg → dir → cert →
   permisos cert/key → despliega script (`template`, `no_log`) → despliega
   unidad (`copy`) → `enable iac-rdp-config.service` (+`daemon_reload` si la
   unidad cambió) → lee `grdctl --system status` (idempotencia) → si no está
   ya `enabled`: parar daemon → ejecutar `iac-rdp-config.sh` → arrancar daemon
   → asegurar `started`+`enabled` → **verificación final** (`failed_when` si no
   quedó `enabled` con rutas TLS).
4. **`roles/rdp/defaults/main.yml`**: nueva var `rdp_creds_dir`
   (`/var/lib/gnome-remote-desktop/.local/share/gnome-remote-desktop`).
5. **`roles/rdp/CLAUDE.md`**: reescrito (nueva estrategia + historial de los
   dos bugs 16/17 para no repetirlos).

### Pendiente de validar

Regenerar ISO + primer arranque desatendido y comprobar: (a) `grdctl --system
status` → `Status: enabled` con rutas TLS; (b) `PLAY RECAP ... failed=0` (que
`vscode` y posteriores vuelvan a ejecutarse); (c) que **sobrevive a reboot**;
(d) conexión RDP real con `ubuntu`/`ubuntu`.

> Nota de diseño recordatoria: el rol gestiona la instancia *system* =
> "Inicio de sesión remoto". "Compartición de escritorio" es la instancia
> *usuario* y el rol **no la toca** (que esté OFF es esperado).

## Rol Ansible `rdp` — fix: el playbook se colgaba al habilitar el unit

### Hora: ~15:00

**Síntoma tras desplegar el fix anterior**: el primer arranque se quedó
**clavado** y RDP sin configurar (`Unit status: inactive`, todo `(null)`).

**Diagnóstico (equipo `10.0.72.51`)**:

1. Log crudo `5-PrimerArranque.log`: tras
   `13:37:37 TASK [rdp : Habilitar iac-rdp-config.service …]` **no hay nada
   más** (ni `changed:`, ni `fatal:`, ni `PLAY RECAP`, ni `TASK [vscode`).
   Cuelgue duro en esa tarea.
2. `systemd-analyze verify /etc/systemd/system/iac-rdp-config.service`: sin
   ciclo de orden ni errores (solo warnings ajenos de `xfs_scrub`). El unit
   está bien.
3. `systemctl status`: `iac-rdp-config.service` y
   `gnome-remote-desktop.service` ambos `disabled; inactive` (el play se colgó
   **antes** de la tarea que habilita `gnome-remote-desktop.service`).
4. **El script a mano FUNCIONA**: `systemctl stop gnome-remote-desktop` →
   `bash -x /usr/local/sbin/iac-rdp-config.sh` (`rc=0`) →
   `systemctl start gnome-remote-desktop` → `grdctl --system status` =
   `Status: enabled`, TLS OK, `Password: (hidden)`. La estrategia de desplegar
   ficheros es correcta.

**Causa raíz**: la tarea "Habilitar iac-rdp-config.service" era la **única**
que usaba el módulo `systemd` con `daemon_reload`. `systemctl daemon-reload`,
ejecutado por Ansible **dentro de `3-SetupPrimerInicio.service`** durante el
primer arranque, se **bloquea indefinidamente** (cuelgue clásico systemctl↔
PID1 durante el boot). El módulo `systemd` sin `daemon_reload` (stop /
started+enabled) nunca colgó en runs anteriores.

### Cambios aplicados

1. **`roles/rdp/tasks/main.yml`**: la tarea de habilitar el unit ya **no usa
   el módulo `systemd`**; crea el symlink
   `/etc/systemd/system/multi-user.target.wants/iac-rdp-config.service` con el
   módulo `file` (`state: link`, `force: true`). Es exactamente lo que hace
   `systemctl enable` para `WantedBy=multi-user.target`, pero sin llamar a
   systemd ni a `daemon-reload` → no puede colgarse. systemd lee el symlink en
   el siguiente arranque; este arranque ya se configura ejecutando el script
   directamente en el bloque `parar→script→arrancar`.
2. **`roles/rdp/files/iac-rdp-config.service`**: `WantedBy=` pasa de
   `gnome-remote-desktop.service` a `multi-user.target` (desacoplado del
   propio servicio que ordena `Before=`; mismo patrón que
   `3-SetupPrimerInicio.service`).
3. **`roles/rdp/CLAUDE.md`**: punto 5 e historial de bugs actualizados.

### Pendiente de validar

Regenerar ISO + primer arranque desatendido: (a) el playbook **no se cuelga**
y llega a `PLAY RECAP failed=0`; (b) `grdctl --system status` → `enabled`;
(c) sobrevive a reboot (el `iac-rdp-config.service` lo reafirma); (d) conexión
RDP real con `ubuntu`/`ubuntu`.
