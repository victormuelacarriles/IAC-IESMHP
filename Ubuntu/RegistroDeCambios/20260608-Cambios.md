# Cambios 2026-06-08

## 09:01 — Rol `nvidia`: instalar NVIDIA Container Toolkit si hay GPU

**Fichero**: `Ubuntu/ansible/roles/nvidia/tasks/main.yml` (+ `CLAUDE.md` del rol).

Al final del bloque `when: has_nvidia` (tras comparar el driver recomendado
con el instalado) se añade la instalación del **NVIDIA Container Toolkit**,
solo si hay GPU NVIDIA y solo si no está ya instalado:

1. `dpkg-query` comprueba si `nvidia-container-toolkit` ya está instalado
   (`changed_when: false`, `failed_when: false`).
2. Bloque `when: "'install ok installed' not in nct_installed.stdout"`:
   - Descarga la clave GPG oficial a
     `/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg`
     (`creates:` para no re-descargar).
   - Añade el repo APT a
     `/etc/apt/sources.list.d/nvidia-container-toolkit.list` con `sed`
     para inyectar `signed-by=` (`creates:` idempotente).
   - `apt install nvidia-container-toolkit` con `update_cache: yes` (repo
     recién añadido).
3. `nvidia-ctk cdi generate --output=/etc/cdi/nvidia.yaml` con
   `creates: /etc/cdi/nvidia.yaml` y `failed_when: false` (en instalación
   fresca el módulo NVIDIA puede no estar cargado aún; el reinicio final de
   `3-SetupPrimerInicio.sh` lo activa).

Permite que Docker/Podman accedan a la GPU vía CDI. Idempotente.

## 09:14 — Fix: `curl: not found` en la descarga de la clave/lista del toolkit

**Fichero**: `Ubuntu/ansible/roles/nvidia/tasks/main.yml`.

El equipo no trae `curl` (`/bin/sh: 1: curl: not found`, rc=2). Se sustituye
`curl ... | gpg/sed` por el módulo `get_url` de Ansible (descarga vía Python,
sin depender de `curl`):

- `get_url` baja la clave armored a `/tmp/nvidia-container-toolkit.asc` y la
  lista a `/tmp/nvidia-container-toolkit.list.orig`.
- `gpg --dearmor` (disponible) genera el keyring desde el `.asc`
  (`creates:` idempotente).
- `sed` (disponible) transforma la lista inyectando `signed-by=` y la
  escribe en `/etc/apt/sources.list.d/` (`creates:` idempotente).

## 09:20 — Revertir a `curl` + `curl` en el rol `basicos`

Diferencia de velocidad get_url/curl irrelevante para 2 ficheros diminutos
(domina la latencia de red). Se revierte a los comandos `curl` originales de
NVIDIA por consistencia con la doc oficial.

- **`Ubuntu/ansible/roles/basicos/tasks/main.yml`**: nueva tarea
  `apt: curl state=present` (el usuario la añadió). `basicos` es el 1er rol
  de `roles.yaml`, así que `curl` queda disponible para `nvidia`.
- **`Ubuntu/ansible/roles/nvidia/tasks/main.yml`**: las dos tareas de
  descarga vuelven a `curl ... | gpg --dearmor` y
  `curl ... | sed ... > .list` (con `creates:` idempotente).
- **`Ubuntu/ansible/roles/basicos/CLAUDE.md`**: documentado `curl` (y `git`,
  que faltaba) en la lista de paquetes; "cuatro tareas" → "seis tareas".

## 09:30 — Fix: `NO_PUBKEY` / keyring NVIDIA vacío por run fallida previa

**Fichero**: `Ubuntu/ansible/roles/nvidia/tasks/main.yml`.

Síntoma: `apt update` falla con `NO_PUBKEY DDCAE044F796ECB0` /
`repository ... is not signed`, y la tarea de descarga de la clave sale `ok`
(saltada) en vez de `changed`.

Causa: la **primera run fallida** (sin `curl`) ejecutó igualmente
`gpg --dearmor`, que **creó el keyring
`/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg` VACÍO**. El
`creates:` de la tarea de descarga lo daba por válido y saltaba la
regeneración → keyring vacío → apt no puede verificar la firma del repo.

Fix:
- Se quita el `creates:` del keyring (no distingue "existe" de "es válido").
- Nueva tarea `gpg --show-keys <keyring>` (`failed_when: false`) que detecta
  si el keyring contiene una clave válida.
- La descarga ahora corre `when: nct_keyring_check.rc != 0` (solo si falta o
  está vacío/corrupto → self-healing en máquinas que ya tienen el keyring
  malo), con `set -o pipefail` (bash) para que el pipe falle si `curl` falla
  y `gpg --dearmor --yes` para sobreescribir el fichero inválido.

## 09:45 — Nuevo rol `flatpak` (soporte Flatpak + Flathub)

**Ficheros**: `Ubuntu/ansible/roles/flatpak/tasks/main.yml` (nuevo),
`Ubuntu/ansible/roles/flatpak/CLAUDE.md` (nuevo), `Ubuntu/ansible/CLAUDE.md`
(tabla de roles actualizada) y `Ubuntu/ansible/roles.yaml` (la entrada del
rol, activa tras `nvidia`).

El rol deja el equipo listo para instalar aplicaciones Flatpak:

1. `apt install flatpak gnome-software-plugin-flatpak` (`update_cache: false`,
   la caché ya se refresca en `pre_tasks`).
2. Comprueba los remotos con `flatpak remotes --system` (`changed_when: false`).
3. Añade el remoto `flathub` con
   `flatpak remote-add --if-not-exists flathub <flatpakrepo>` a nivel de
   **sistema**, solo si no estaba (idempotente vía `when`/`changed_when`).

## 09:52 — Corrección de errata: `flatpack` → `flatpak`

El rol se había creado con la errata `flatpack`. Renombrada la carpeta
`roles/flatpack` → `roles/flatpak` y corregidas todas las apariciones de
`flatpack` (en `roles.yaml`, `Ubuntu/ansible/CLAUDE.md`, los dos ficheros del
rol y este registro). Ahora el nombre del rol coincide con el del paquete.
